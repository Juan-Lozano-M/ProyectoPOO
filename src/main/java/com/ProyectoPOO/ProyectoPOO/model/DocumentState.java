package com.ProyectoPOO.ProyectoPOO.model;

// `state` = estado del documento asociado al vehiculo.
public enum DocumentState {
    HABILITADO,      // Documento vigente y habilitado.
    VENCIDO,         // Documento vencido.
    EN_VERIFICACION  // Estado inicial en proceso de verificacion.
}
